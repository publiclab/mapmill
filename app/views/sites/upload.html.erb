<div id="failure" class="alert alert-danger" role="alert" style="display:none">Oooops - looks as something didn't go well...
<button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
  </div>


<h2 style="display: inline-block; margin-right: 20px"><%= @site.name %>: Upload Page</h2><div class="site_img_num"><%= @site.images.count %> images</div><a style="margin-left: 20px; padding: 10px; margin-bottom: 10px; color: white !important" class="btn btn-primary" href="/sites/<%= @site.id%>">List View of images</a>
 

<div style="display:none" id="s3-data" data-host='<%= @s3_post_data[:host] %>' data-fields='<%= @s3_direct_post.fields.to_json.html_safe %>' data-url="<%= @s3_post_data[:url] %>"></div>
<input type="hidden" id="site_id" value="<%= @site.id %>"/>
<input type="hidden" id="site_name" value="<%= @site.name %>"/>
<div class="upload_section">
  <div id="upload-status"></div>
  <div id="upload-area">    
    <div class="row">
    <h4 class="upload-text">1) Drag & drop to add images, or <a href="#" id="href-upload">choose images</a></h4>
      <div id="upload-list">
      </div><!-- upload-list-->
    </div><!-- row -->
  </div><!-- upload-area -->
</div><!-- upload-section -->

<h4 class="upload-text">2) Filter: Select some images:</h4> <span class="small_text">This is a chance to filter out some images before you start using A LOT of bandwidth!</span>
<div id="no-files" class="alert alert-warning" role="alert" style="display:none">Please do select some files to be uploaded!</div>
<!-- The global progress state -->
<div class="progress-section fade">
  <!-- The global progress bar -->
  <div class="progress-area">
      Upload overall progress status:<br />
    <div class="progress-bar progress-bar-success" aria-valuemax="100" aria-valuemin="0" aria-valuenow="0" role="progressbar">
      0%
    </div>
  </div>
  </div>
</div> 
<div class="container">
  <div id='upload-candidates-container' class='row'>   
  </div>
</div> 
<!--this form has to do with the file upload functionality-->
<form enctype="multipart/form-data" method="POST" id="fileupload">
    <!-- Redirect browsers with JavaScript disabled to the origin page -->
    <noscript>&lt;input type="hidden" name="redirect" value="http://localhost"&gt;</noscript>
    <div class="row fileupload-buttonbar">
        <div class="col-lg-7">
            <!-- The fileinput-button span is used to style the file input field as button -->
            <span class="btn btn-success fileinput-button hidden">
                <i class="glyphicon glyphicon-plus"></i>
                <span>...or browse for images</span>
                <input style="width: 100%" type="file" multiple="" name="files[]" id="fileinput">
            </span>
            <div class="upload-buttons-section">
                    
            </div>
        </div>        
</form>

<!--<div id="upload-msg" class="status-messages"> </div>-->

<% content_for :footer do %>
  <footer>
    <div class='container'>
      <button id="upload_selected" class="btn btn-success start" type="submit">
          <i class="glyphicon glyphicon-upload"></i>
          <span>Start upload</span>
      </button>
      <h4 class='upload-text'>3) Finally, upload them! </h4> 
    </div>                
  </footer>
<% end %>

